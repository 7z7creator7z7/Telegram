services:
  - type: web
    name: telegram-zero
    runtime: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: telegram-zero-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
databases:
  - name: telegram-zero-db
    plan: free

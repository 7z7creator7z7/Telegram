services:
  - type: web
    name: telegram-app
    runtime: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: telegram-db
          property: connectionString

databases:
  - name: telegram-db
    plan: free
